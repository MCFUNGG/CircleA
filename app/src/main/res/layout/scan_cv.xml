<?xml version="1.0" encoding="utf-8"?>
<!-- scan_cv.xml : Layout for scanning CV -->
<androidx.constraintlayout.widget.ConstraintLayout 
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <!-- Image Preview -->
    <ImageView
        android:id="@+id/imageView"
        android:layout_width="0dp"
        android:layout_height="200dp"
        android:scaleType="centerCrop"
        android:contentDescription="Image Preview"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- Button to select image -->
    <Button
        android:id="@+id/selectImageBtn"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Select Image"
        app:layout_constraintTop_toBottomOf="@id/imageView"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="16dp"/>

    <!-- Contact Input -->
    <EditText
        android:id="@+id/contactEditText"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:hint="Contact"
        app:layout_constraintTop_toBottomOf="@id/selectImageBtn"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="16dp"/>

    <!-- Skills Input -->
    <EditText
        android:id="@+id/skillsEditText"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:hint="Skills"
        app:layout_constraintTop_toBottomOf="@id/contactEditText"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="8dp"/>

    <!-- Education Input -->
    <EditText 
        android:id="@+id/educationEditText"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:hint="Education"
        app:layout_constraintTop_toBottomOf="@id/skillsEditText"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="8dp"/>
        
    <!-- Language Input -->
    <EditText
        android:id="@+id/languageEditText"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:hint="Language"
        app:layout_constraintTop_toBottomOf="@id/educationEditText"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="8dp"/>

    <!-- Other Input -->
    <EditText 
        android:id="@+id/otherEditText"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:hint="Other"
        app:layout_constraintTop_toBottomOf="@id/languageEditText"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="8dp"/>

    <!-- Save Button -->
    <Button
        android:id="@+id/saveButton"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Save"
        app:layout_constraintTop_toBottomOf="@id/otherEditText"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="16dp"/>
        
</androidx.constraintlayout.widget.ConstraintLayout>